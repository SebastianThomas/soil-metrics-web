# prod build - same origin as the SPA (no CORS). soil-metrics-web.sthomas.ch
# path-routes /v1 and /public to the backend (see deploy/httproute.yaml).
VITE_API_URL=https://soil-metrics-web.sthomas.ch
