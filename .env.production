# prod build - the API + datasets host (start-hack-backend repo). CORS for
# /v1/** and /public/** is `*` there. Both soil-metrics.sthomas.ch and
# soil-metrics-web.sthomas.ch serve this SPA and call out to here.
VITE_API_URL=https://soil-metrics-ws.sthomas.ch
